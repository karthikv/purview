version: v1.0
name: Purview
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Build"
    task:
      prologue:
        commands:
          - checkout
          - npm install

      jobs:
      - name: Lint
        commands:
          - bin/lint

      - name: Test
        commands:
          - bin/test
